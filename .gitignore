*.gem
*.rbc
/.config
/coverage/
/InstalledFiles
/pkg/
/spec/reports/
/spec/examples.txt
/test/tmp/
/test/version_tmp/
/tmp/
node_modules/
/src/modules/llama/models
/src/modules/llama/llama.log

# Used by dotenv library to load environment variables.
# .env

# Sensetive project files:
/config.yml
/ecosystem.config.js
/responses.example
/src/events/*
!/src/events/evt_manual.rb
!/src/events/evt_wrapper.sh

# Ignore Byebug command history file.
.byebug_history

## Specific to RubyMotion:
.dat*
.repl_history

## Documentation cache and generated files:
/.yardoc/
/_yardoc/
/doc/
/rdoc/

# Ignore bundler config.
/.bundle
/webview/.bundle

## Environment normalization:
/.bundle/
/vendor/bundle
/lib/bundler/man/

# for a library or gem, you might want to ignore these files since the code is
# intended to run in multiple environments; otherwise, check them in:
# Gemfile.lock
# .ruby-version
# .ruby-gemset

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

# OS-specific files:
*.DS_Store
*\:Zone.Identifier

# Ignore all environment files (except templates).
/webview/.env*
!/webview/.env*.erb

# Ignore all logfiles and tempfiles.
/webview/log/*
/webview/tmp/*
!/webview/log/.keep
!/webview/tmp/.keep

# Ignore pidfiles, but keep the directory.
/webview/tmp/pids/*
!/webview/tmp/pids/
!/webview/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/webview/storage/*
!/webview/storage/.keep
/webview/tmp/storage/*
!/webview/tmp/storage/
!/webview/tmp/storage/.keep

/webview/public/assets

# Ignore master key for decrypting credentials and more.
/webview/config/master.key
